# BIS 0.0.5 local package release

Adds a read-only Support section to the Developer page. Asset Minting, Contracts, and Items display their labels with disabled checkboxes sourced from the three `BisGameServices.has...Support()` methods. The section reflects current wallet/capability state whenever the Developer page is loaded and when subscribed wallet state changes.
