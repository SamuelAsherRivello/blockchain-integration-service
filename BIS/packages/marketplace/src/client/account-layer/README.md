# Account layer

This layer is reserved for Marketplace integration points with BIS accounts and wallets. Keep Marketplace catalog logic in `marketplace-layer` and reusable inventory calculations in `inventory-layer`.
