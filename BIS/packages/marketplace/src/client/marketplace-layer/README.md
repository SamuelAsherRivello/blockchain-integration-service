# Marketplace layer

This layer owns Marketplace list, detail, and checkout orchestration. Keep inventory normalization in `inventory-layer`, account integration in `account-layer`, and styling/assets in `ui-layer-react`.
