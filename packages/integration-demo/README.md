# Demo application

Consumes the integration package through its public exports.

- `src/admin`: development panel.
- `src/preview`: 9:16 host viewport.
- `src/App.tsx`: split-screen composition.

The preview mounts the package's production UI. It must not duplicate account or payment components.
